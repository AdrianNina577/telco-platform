Configuration:
  status: info
  name: Default
  appenders:
    Console:
      name: LogToConsole
      PatternLayout:
        Pattern: "%d{HH:mm:ss.SSS} [%t] %-5level %logger{36} - %msg%n"
  Loggers:
    root:
      level: info
      AppenderRef:
        - ref: LogToConsole
